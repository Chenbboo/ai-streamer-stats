# 项目管理金额：服务器数据同步与复测（2026-09-24）

本次按用户要求，将服务器业务库同步到本地后重新检查。修复前基线为 `codex/three-system-product / a7f5b00d`。下文保留修复前的审计结果；用户确认后的本地修复见文末“修复结果”。没有调整来源单据、回填金额或部署。

## 同步结果

- 来源：服务器应用配置对应的 `ry_live`，使用 `mysqldump --single-transaction --quick --skip-lock-tables` 导出；171 张表均为 InnoDB。
- 目标：本机 `ry-vue`。导出约北京时间 10:40，10:41:44 完成本地替换。
- 快照包含 **171 张表、45,841 行**。先恢复到本地临时库，再替换本地库；表行数、字段定义及每张表全部记录的内容哈希均一致。
- 原本地库先完整备份，并恢复到独立库验证可恢复，保留了 152 张表、42,039 行原记录。
- 备份与同步证明：`work/db-backups/amount-audit-20260924_103943/`，包含 `local-before-sync.sql`、`server-snapshot.sql` 和 `manifest.json`。
- 原库的可用恢复副本：`amount_audit_before_20260924_104020`。服务器快照校验副本：`amount_audit_server_20260924_104020`。
- 没有停止、重启或修改服务器应用，没有运行服务器迁移。复测未启动本地应用或后台任务。

## 已在真实数据中确认的问题

### 1. DEWI 的每月预测重复显示整个项目金额

正式项目 20 / 立项 19，计划期间 2026-09-12 至 2026-12-11。恢复库中保存的 9、10、11、12 月预测均为：

| 月份 | 预计收入（元） | 预计成本（元） | 预计利润（元） | 保存的期间 |
| --- | ---: | ---: | ---: | --- |
| 9 月 | 71,966.67 | 27,958.25 | 44,008.42 | 9/12—12/11 |
| 10 月 | 71,966.67 | 27,958.25 | 44,008.42 | 9/12—12/11 |
| 11 月 | 71,966.67 | 27,958.25 | 44,008.42 | 9/12—12/11 |
| 12 月 | 71,966.67 | 27,958.25 | 44,008.42 | 9/12—12/11 |

这四行实际都取了全项目测算，而不是对应月的测算。项目中 47,700 元固定服务费明确是 9 月的一次性预计收入，不应在后续三个月再次出现。

按当前代码的每月收入以 30 天折算规则，9—12 月正确收入分别为 **52,766.67 / 8,266.67 / 8,000.00 / 2,933.33 元**，合计仍为 71,966.67 元。已用当前预算源码及独立算式双重核对，没有改动计划中的收费规则。

根因仍为 `BusinessProjectBudgetService.java:93` 指定月度后，`:123` 又因存在结束日而强制使用全项目期间。此问题影响预测展示及保存的预测快照；并不表示真实收入已重复入账。

### 2. 昨日花费少包含 329.02 元公共费用

按 2026-09-23 的实际记录，以下四个项目的奖金、业务成本均为 0，但已计入公共费用。工作台目前仅相加业务成本和人员成本，遗漏公共费用：

| 项目 | 工作台公式金额（元） | 同日核算总成本（元） | 漏掉的公共费用（元） |
| --- | ---: | ---: | ---: |
| 情趣项目 | 142.89 | 192.24 | 49.35 |
| 出海短剧 | 106.53 | 122.98 | 16.45 |
| 新谷酵素视频制作 | 179.25 | 310.86 | 131.61 |
| 法沁颜视频制作 | 361.07 | 492.68 | 131.61 |
| 合计 | 789.74 | 1,118.76 | **329.02** |

这些公共费用已正确存在于核算日结果，错误位于负责人工作台的金额口径。根因为 `BusinessProjectServiceImpl.java:3263`、`:3291`。

已将恢复库的真实经营事实和人员日明细注入既有测试夹具，直接调用当前编译的 `ownerWorkbench` 服务，四项返回值与上表一致。这是服务级回放，未进行 HTTP 或浏览器操作。证据为 `server-retest/owner-workbench-replay.json`，回放也确认奖金同样会遗漏。

## 已入账金额对账

检查全部 **124 条当前有效日结果**，其中 67 条属于 11 个未删除项目。以下检查差异均为 0：

- 收入、业务成本、奖金、调整与已生效经营事实一致，包括原单和冲销单的净额。
- 人员成本与已计价人员日记录一致；另取 4 条真实人员成本，按保存的月度成本、分摊比例、工作日及分币尾差独立重算，全部一致。
- 公共费用与每日分摊来源一致；公共月账、负责人分配和已提交项目分配金额一致。
- 利润公式、日结果金额分项、预算累计一致。
- 无同项目同日多条同时有效结果；已有生效收支及到期公共费用没有缺失相应日结果。

实际冲销样本：卫龙 9/22 的支出为原单 43,692,648 越南盾、冲销 -43,692,648 越南盾、重录 43,692,648 越南盾，最终业务成本净额为 43,692,648 越南盾，没有重复记为两笔支出。不同币种分别核对，没有将人民币与越南盾相加。

## 数据尚未完成的部分

- 当前 11 个未删除项目没有人员成本待计价记录。全库 306 条待计价均属于已删除项目 1、3，不能当作当前项目缺项。
- 公共费用月账 46,066.66 元，其中 **41,459.99 元尚未由负责人提交到项目分摊**；已提交 4,606.67 元，截至 9/24 按日计入 2,632.16 元。未提交分摊属于待办理业务，不能称为算术错误，也不能据已入账结果认定全部项目成本已经完整。
- 当前没有内部拨款实例，不能声称已经对真实拨款金额完成实例验证。

## 上轮发现的其他问题

有限期项目合法的筹备支出/尾款漏入总预计、关账前后计税范围变化、遗留接口重复去重人员成本，构造场景重跑仍能复现。

不过，恢复的真实数据中未发现执行期外一次性收支；公司税率配置和税冻结快照均为 0 条。因此这些暂列为代码边界问题，不能声称本批实际账目已经产生对应错账。遗留人员汇总字段仍没有被当前前端使用。

## 验证与证据

- 后端核算、Mapper、币种、成员日成本、税额、项目预算及管理费共 **123 项测试通过**，无失败、错误或跳过。
- 前端预算及项目层级工具测试 **6 项通过**。
- 4 个独立构造场景已基于本轮 Maven 编译结果重跑。它们用于重现边界问题，不代替实际数据对账。
- 复测完成后再次核对全部 171 张表的内容哈希，与导入时完全相同；测试未改变本地快照。证明为 `server-retest/post-test-integrity.json`。
- 只读对账脚本、查询、真实预算快照及结果：`logs/amount-audit-20260924/server-retest/`。
- 同步、备份和核对过程没有修改业务源码。SQL、备份和日志均位于忽略目录，未提交或推送。

本报告的服务器快照结论取代上一轮旧本地库的数据范围和待计价数量；上一轮代码分析仍作为问题根因记录保留。

## 修复结果（用户确认后）

本次只修复已确认的两项金额问题，变更仍在本地工作区，未提交、推送或部署。

1. **月度预测**：独立按自然月与项目、人员参与期间求交，收入、业务支出、人员成本和利润使用对应月份的金额；一次性收支和内部拨款只进入发生月份。月度收支的分币尾差与全期合计对齐。
2. **昨日花费**：统一为业务成本、人员成本、奖金、公共费用之和。公共费用与日核算使用相同公式，扣除已被日分摊替代的月结事实，避免重复计费。负责人页面的总览、列表、单项目显示四类分项，中越文均已补齐。

已有立项详情、项目当前计划、历史基线和变更详情均接入只读月表修正。历史 `snapshotJson`、审批总额和数据库不变；原人员依据与当前重建结果必须同时满足费率版本/工作日依据及全期金额一致，才展示历史分月人员成本。依据不足时显示待完善及原因。新测算月表带版本标记，后续读取不重新计价。不限期项目保留原先只动态刷新稳定月预测的行为。

### 真实记录修复后结果

DEWI（人民币）：

| 月份 | 收入 | 人员/总成本 | 利润 |
| --- | ---: | ---: | ---: |
| 2026-09 | 52,766.67 | 5,539.77 | 47,226.90 |
| 2026-10 | 8,266.67 | 9,375.00 | -1,108.33 |
| 2026-11 | 8,000.00 | 9,375.00 | -1,375.00 |
| 2026-12 | 2,933.33 | 3,668.48 | -735.15 |
| 合计 | 71,966.67 | 27,958.25 | 44,008.42 |

情趣项目的 7,978.13 元一次性业务支出仅出现于 7 月；7—10 月人员成本分别为 15,108.69 / 16,613.10 / 13,375.00 / 4,863.64 元，合计 49,960.43 元，与原审批总额一致。两项目实际人员依据均可匹配；构造费率版本变化或缺历史金额时，服务会保留收支月额并将人员、总成本和利润标为未知。

9/23 四个项目的“昨日花费”修复后分别为 192.24 / 122.98 / 310.86 / 492.68 元，合计 **1,118.76 元**，与日核算一致，补齐了原来遗漏的 **329.02 元**。合成场景还验证了奖金、公共月结去重和冲销。

### 修复验证

- 后端覆盖预算、项目计划、立项、负责人工作台和关联核算服务的 **453 项聚焦回归全部通过**，无失败、错误或跳过。详细结果为 `server-retest/backend-tests-after-fix.json`；日志为 `server-retest/backend-tests-after-fix.log`、`server-retest/budget-final-tests.log`、`server-retest/plan-final-tests.log`。集成用例覆盖旧快照时间戳日期转为有效业务日期后，真实预算服务返回正确月额及 READY 状态。
- 前端预算/项目层级工具 6 项和语言工具 4 项通过；修改的 Vue 组件编译、历史快照展示数据选择验证通过；`pnpm run build:prod` 通过。
- 全局 `check:i18n` 仍报告 3 条原有缺失词条：“经营回顾公司”“当前公司：{0}”“请选择所属部门”。本次新增文本均有越南语翻译。
- 真实服务回放证据：`server-retest/real-budget-projection-after.json`、`server-retest/real-plan-projection-after.json`、`server-retest/owner-workbench-replay-after-fix.json`。两项目当前计划与历史展示月表逐字段一致，均为 READY 且无问题提示，原快照 JSON 原文不变。回放使用服务器同步数据，未启动实际应用、HTTP 服务或后台任务。
- 修复后复核本地全部 **171 张表**，每张内容哈希仍与导入时一致；证明为 `server-retest/post-fix-integrity.json`。
- 待办理的 41,459.99 元公共费用分摊及前述其他边界问题未纳入本次两项修复。
